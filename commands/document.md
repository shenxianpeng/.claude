Add or update documentation for the following code: $ARGUMENTS

Please:
1. Write a module-level docstring (if the file lacks one) describing the overall purpose
2. Add or improve docstrings for every public function, method, and class:
   - One-line summary
   - `Args:` / `Parameters:` block with name, type, and description for each parameter
   - `Returns:` block describing the return value and type
   - `Raises:` block listing exceptions that can be raised
3. Add inline comments for any logic that is non-obvious or tricky
4. Follow the project's existing docstring style (NumPy, Google, or reStructuredText);
   default to **Google style** if no convention is established

Do **not** change any logic — documentation changes only.
