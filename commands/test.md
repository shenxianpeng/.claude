Generate comprehensive tests for the following code: $ARGUMENTS

Please:
1. Identify all public functions, methods, or components that need tests
2. Write unit tests covering:
   - Happy path (expected inputs and outputs)
   - Edge cases (empty input, boundary values, large input)
   - Error cases (invalid input, thrown exceptions)
3. Follow the existing test style and framework already used in this project
4. Co-locate the test file next to the source file using the project's naming convention (e.g. `foo.test.ts` or `test_foo.py`)
5. Mock external dependencies (network calls, databases, file system) where appropriate

Output the complete test file ready to be saved and run.
